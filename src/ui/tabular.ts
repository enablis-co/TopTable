/**
 * The opt-in tabular-figure utility class defined in `base.css`
 * (`font-variant-numeric: tabular-nums`). Import this rather than typing the bare string
 * 'tt-num' from memory — every element that renders a number that updates (capacity, seat
 * counts, totals) needs it, or the value jitters as it changes.
 */
export const tabularClass = 'tt-num'
